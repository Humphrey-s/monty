#include <stdio.h>
#include "monty.h"
#include <stdlib.h>
#include <unistd.h>
/**
 * _nop - swap the top 2 elements of a stack
 * @stack: stack
 * @line_number: line number
 * Return: (Success)
 */
void _nop(UNUSED stack_t **stack, UNUSED unsigned int line_number)
{
}
